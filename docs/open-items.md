# דברים שלא לשכוח

מסמך זה הוא רשימה שטוחה בלבד, לא תיאור משימות ולא סטטוס שלבים. כל שורה היא דבר קטן שעלה תוך כדי עבודה, לא חוסם את המשימה הנוכחית, אבל אסור שייפול בין הכיסאות. ראה את החוק המחייב ב-`docs/dev-manager-prompt.md`, סעיף "רשימת הדברים שלא לשכוח".

כשפריט נסגר בפועל ונבדק, למחוק אותו מכאן, לא להשאיר מסומן `[x]`, זו לא רשימת משימות.

- **פריסת `.layout-rail` הכי לחוצה בדיוק ברגע שהיא נכנסת, ואף אחד לא מדד שם.** מ-1024 המסילה לוקחת 320 קבועים ועוד 32 מרווח, ולכן העמודה הראשית מקבלת רוחב החלון פחות 712. **נמדד בפועל על 1024: העמודה הראשית 312 מול מסילה 320**, כלומר הצד המשני רחב מהעיקרי, והתוכן מקבל שם פחות רוחב מאשר בטלפון של 390 (שם הוא 342, כי הניווט קורס לכותרת עליונה והריפוד יורד ל-24). זה חל על שלושה מסכים: כסף, יומן ויומן ריסוס. **התסמין תוקן ברשומות ריסוס בלבד** (שורת הפרטים נשברת לשתיים במקום להיחתך, ראה `.log-row__meta--wrap`), הסיבה לא. **שתי הצעות תיקון, ואחת מהן לא עובדת:** `minmax(240px, 320px)` על עמודת המסילה **הוא no-op**, כי בגריד טראק לא-גמיש מקבל את ה-max שלו בשלב Maximize Tracks לפני שטראק ה-`fr` מקבל את השארית, ולכן המסילה תישאר 320 והעמודה הראשית 312. מה שכן עובד: להעביר את `.layout-rail` מ-`@media (min-width: 1024px)` ל-`@container (min-width: 840px)` עם `container-type: inline-size` על `.shell__content`. 840 הוא הרוחב שבו העמודה הראשית מקבלת 488, שלוש נקודות השבירה נשארות שלוש כי 840 אינו רוחב חלון, וזה גם מה שיגרום למסילה לעבוד נכון אם אי פעם תושתל בתוך טאב של פרטי חלקה. **אזהרה לפני שמיישמים:** `container-type` מחיל `contain: layout`, שהופך את `.shell__content` למכל של צאצאים ב-`position: fixed`. `Modal.css` בנוי על `position: fixed; inset: 0`, ולכן חייבים לוודא בדפדפן שכל הגיליונות עדיין נפרסים על החלון ולא על עמודת התוכן. **בכל בדיקת רספונסיביות בהמשך למדוד גם ב-1024, לא רק ב-1536, 900 ו-390.**
- **רצפת המגע של 48 לא מתקיימת בשישה מקומות, וכולם מלפני 2026-08-31.** `grep "calc(var(--touch-min)"` מחזיר חמישה כללים: `ExportBar.css:27` (32), ובפרטי חלקה `PlotDetailScreen.css` שורות 122 (`.tab-track__item`, 36), 183 (`.plot-detail__identity-edit`, 32), 237 (`.plot-detail__forecast-update`, 32) ו-254 (`.plot-detail__spray-log-link`, 32). בנוסף שתי תיבות הסימון ב-`SettingsScreen.css` הן 20 על 20. `design.md` קובע 48 מינימום גם בווב, במפורש, כי זה אותו קהל בן 55 עד 70. **הנקודה האמיתית היא לא הגובה אלא הדפוס:** חמישה אתרים מנטרלים טוקן משותף בחשבון אד-הוק. או שהמוצר מכבד 48, או שיש טוקן משני אחד בעל שם ונימוק כתוב (למשל `--touch-compact`) שמותר להשתמש בו במודע. לא תוקן בסבב הרספונסיביות כי זה גובה ולא רוחב, ו-`ExportBar` ו-`.tab-track` מרונדרים בכמה מסכים.
- **המספר הגדול במסך הבית כבר בולע את הריפוד שלו ב-390.** `.profit-hero__value` נושא `white-space: nowrap`, ונמדד בפועל על 390: `scrollWidth` 313 מול `clientWidth` 300. כרגע אין נזק נראה, שום תו לא נחתך ואין גלילה אופקית, כי הטקסט חורג לתוך הריפוד של הכרטיס. עם סכום ארוך יותר, למשל שבע ספרות עם אגורות, זה כן ייחתך. הכיוון כשנוגעים: גודל גופן נזיל לגיבור מתחת ל-700, ולא ביטול ה-nowrap, כי שבירת מספר כספי לשתי שורות גרועה יותר. **ולתקן את שניהם באותה נשימה**: `.plot-profit` בפרטי חלקה הוא שכפול מחלקה-מול-מחלקה של `ProfitHeroCard` (אותם `__label`, `__value` עם שלושת המודיפייארים, `__glyph`, `__breakdown`, `__caveat`, ואותו נימוק Wheat מילה במילה), ול-`.profit-hero__value` יש `clamp()` של-`.plot-profit__value` אין. כלומר אותה גלישה תופיע גם שם, ומאז 2026-08-31 השניים גם נפרסים אחרת מ-1024 (רצועה מול ערימה) בלי הבדל מוצהר. שתי תצוגות רווח כמעט זהות ששתיהן מדממות זו הזמנה לאחד אותן לרכיב אחד עם וריאנט.
- **"יותר מדי לחיצות" עדיין פתוח, החלטת היזם 2026-08-31.** קיצורי הדרך שנוספו למסך הבית בנייד חוסכים לחיצות במסך אחד בלבד. השאלה כמה לחיצות באמת נדרשות כדי לרשום הוצאה על חלקה, לסגור משימה, או לראות רווח של חלקה מסוימת, לא נבדקה. **זו ארכיטקטורת מידע ולא טיפוגרפיה**, ושום שינוי בטוקנים לא יזיז אותה. סבב נפרד שבו עוברים מסלול-מסלול וסופרים בפועל.
- **OedooPro נשאר ב-`packages/assets/fonts` ואינו בשימוש.** המשפחה הוחלפה ל-Alef ב-2026-08-31. הקבצים לא נמחקו כדי לא לאבד רכישה בתשלום אם תהיה חזרה אחורה. למחוק כשההחלטה יציבה.
- Apple Sign-In חסום, לא נדחה. ממתין לחשבון Apple Developer בתשלום. אין לחזור ולשאול על זה עד שהיזם יגיד שהחשבון נפתח.
- מסך הכניסה, להחליף את אייקון הנבט/הצח הגנרי באנימציית Lottie מתוך הרישיון האישי של היזם ב-LottieFiles. רק במסך הכניסה, כאלמנט חד פעמי או לופ עדין, לא באייקונים חוזרים כמו טאבים או כפתורים. לטפל בשלב הליטוש החזותי, לא לפני כן.
- מחיקת חשבון משתמש אמיתית לצורכי רגולציה. כרגע `farm_members_user_id_fkey` חוסם מחיקת שורה מ-`auth.users`, המוצר כולו בנוי סביב soft delete, אבל בשלב מסוים תידרש נקודת קצה אמיתית למחיקת חשבון על כל הנתונים שלו. לא דחוף כרגע, רק לזכור שקיים.
- `design.html` עדיין מציג את פריסת חמשת הטאבים השטוחה הישנה בנייד, לפני כפתור הרישום המרכזי. הוחלט במכוון לא לפתוח משימת עדכון נפרדת כרגע, `design.md` הוא מקור האמת. לעדכן את `design.html` שיתאים כשמגיעים לעבודת ליטוש כללית על התיעוד.
- רשימות הערכים ב-`packages/shared/src/settings.ts` חייבות להישאר מסונכרנות עם הדומיינים ב-`backend/supabase/migrations/20260824070000_value_domains.sql`, `currency_code`, `area_unit` ו-`locale_code`. כשמוסיפים מטבע או שפה, מוסיפים בשני המקומות, ולעולם לא רק בקליינט. בצד המסד זו כבר מיגרציה אחת שנוגעת בדומיין ולא ציד אחרי כל טבלה שמשתמשת בו.
- הגדרות המשק נשמרות בשני עדכונים נפרדים, `farms.name` ואז `settings`, ולא בטרנזקציה אחת. שניהם מאחורי אותה בדיקת RLS בדיוק, ולכן כשלון חלקי אפשרי רק אם הרשת נופלת בין השניים, והמשתמש פשוט שומר שוב. אם בעתיד יתווסף שדה שכשלון חלקי בו כן מסוכן, להעביר את השמירה לפונקציית Postgres אחת.
- `crop_cycles.name` ו-`tasks.title` הם שמות ישויות וראוי להם הדומיין `public.entity_name`, אבל שתיהן מוגשות דרך `crop_cycles_view` ו-`tasks_view` ו-Postgres חוסם שינוי טיפוס של עמודה שתצוגה תלויה בה. להמיר אותן כשנוגעים בתצוגות האלה ממילא, ולא לפתוח בשבילן הפלה ובנייה מחדש של תצוגה שנושאת מיסוך לפי תפקיד.
- כל מסך טופס מלא חדש בנייד חייב לעבור דרך `FormScreen` (`frontend/mobile/src/components/FormScreen.tsx`), בדיוק כמו שכל גיליון תחתון עובר דרך `BottomSheet`. שם יושב טיפול המקלדת, ומסך שלא עובר דרכו יחזיר את הבאג של שדה שנחתך מתחת למקלדת. שני המסכים הקיימים (`PlotFormScreen`, `SettingsScreen`) כבר מומרים.
- `softwareKeyboardLayoutMode` ב-`app.json` מוגדר `"pan"`. אם יתגלה שמסך טופס מלא עדיין מתנהג לא נכון עם המקלדת באנדרואיד, הכפתור היחיד שכדאי לנסות הוא `"resize"`, שהוא הצימוד הקנוני ל-`KeyboardAvoidingView`. לא הוחלף כרגע כדי לא לערער את הגיליונות שכבר אומתו על מכשיר.
- גיליון עדכון הצפי מציג שדה שלישי, יחידת יבול, **רק כשהיחידה עדיין לא הוגדרה**. `design.md`, Forecast Update, אומר "Two fields, nothing else". החריגה מכוונת ומצומצמת: היחידה אופציונלית בסכמה ונערכת במסך אחר (זהות הגידול), ובלעדיה שני המספרים בגיליון חסרי משמעות ("300 של מה?"), שזה בדיוק הבלבול שדווח. חקלאי שכבר הגדיר יחידה רואה בדיוק שני שדות. לעדכן את `design.md` שיתאר את ההתנהגות המותנית כשמגיעים לליטוש התיעוד.
- **לוח השנה נבנה ולא נבדק על מכשיר.** שני שדות היום/חודש הוסרו מכל מקום בשני הלקוחות והוחלפו ברשת חודש (`packages/shared/src/calendar.ts` + `Calendar.tsx` בנייד ובווב). `@react-native-community/datetimepicker` עדיין נדחה, ועכשיו משלוש סיבות ולא מאחת: תלות native חדשה שדורשת אימות ב-Expo Go (ההיגיון של `BottomSheet.tsx` לגבי reanimated), בורר מערכת לא ניתן לשיתוף עם הווב שסובל מאותה תלונה, והיזם ביקש במפורש משבצות. **מה שנשאר פתוח הוא מכשיר:** רוחב תא יום יוצא כ-45 עד 49 נקודות בתוך גיליון תחתון, כלומר מתחת ל-48 של `touchTarget.min` ברוחב אחד מהשניים (הגובה מוחזק על 48), ואף אחד לא לחץ על זה באצבע של בן 65. לבדוק גם שכותרות ימי השבוע יושבות מעל העמודות הנכונות בפועל, אף שהיישור מובטח בבדיקה ב-`calendar.test.ts`.
- שורת משימה שעברה את התאריך (`TaskRow`) לא ממשת את ההתנהגות המיוחדת שב-`design.md`, "Overdue Banner": לחיצה על שורה באיחור פותחת כרגע את אותו גיליון עריכה כמו כל שורה אחרת, במקום להתרחב inline לשלוש כפתורי פעולה (בוצע / דחה שבוע / לא רלוונטי). הטיפול החזותי (רקע Wheat-100, רצועה מובילה, טקסט תאריך Wheat-800) כן מומש. להוסיף את ההתרחבות inline כשמגיעים לליטוש חוויית המשתמש של הלוח.
- דחיית משימה (snooze) איבדה כל נקודת כניסה ב-UI: ההחלקה (swipe) שהפעילה אותה הוסרה לגמרי מ-`TaskRow` (בקשת חקלאי מפורשת, ראה שיחת ה-UX על "בוצע" בכפתור ברור במקום ג'סטורה). `snoozeTask` ב-`packages/shared/src/tasks.ts` נשאר קיים ונבדק, פשוט אין דרך להפעיל אותו כרגע משום מסך. נקודת הכניסה הטבעית היא ההתרחבות inline של שורה באיחור (הפריט למעלה), שם snooze הוא אחד משלושת הכפתורים ממילא. עד שזה ייבנה, snooze לא נגיש למשתמש בכלל.
- ארכוב אוטומטי (`shouldAutoArchive` ב-`packages/shared/src/tasks.ts`) כותב `archived_at` בשקט, לפי החלטה מכוונת לגרסה הפשוטה בלי פרומפט "עדיין רלוונטי?" (ראה roadmap.md, שלב 3). משימה מאורכבת יוצאת מהלוח אבל אין עדיין שום מסך "ארכיון" שמציג אותה בחזרה, למרות ש-`design.md` מציין "reachable from the board's overflow". לבנות מסך/תצוגת ארכיון כשעולה צורך אמיתי.
- לוח המשימות בבית (`TaskBoard` עם `showPlotName`, בלי `plotId`) מציג כרגע את כל המשימות הפתוחות של המשק בלי הגבלה, לא את חמש השורות הראשונות + קישור "עוד N משימות" שמפרט `design.md`. מתאים כשעובדים על שאר מסך הבית בשלב 4 (Live P&L Hero Card וכרטיסי חלקות), כדי לא לבנות פריסת קיצוץ סביב תוכן שעדיין ישתנה.
- **מכסה שנצרכה על קריאה ש-OpenRouter לא ענה עליה אינה מוחזרת.** `gate` צורך את המכסה **לפני** הקריאה לספק, כי זו הדרך היחידה למנוע מרוץ בין שתי בקשות מקבילות בקצה המכסה. כשהספק נופל, החקלאי כבר שילם רישום אחד מתוך עשרה בחודש ולא קיבל דבר, ו-`consume_ai_quota` יודעת רק להגדיל, אין בה מסלול החזרה. **ההחזר לא נבנה בצעד 6 בכוונה**: הוא דורש מיגרציה חדשה ומסלול הפחתה, ומסלול הפחתה הוא בדיוק סוג הדבר שהופך למשטח ניצול ביום שמשהו אחר יוכל להגיע אליו, בעוד המחיר של הפער הוא הקלטה אחת אבודה מתוך עשר במקרה נדיר. **מה שצריך להכריע כשחוזרים לזה**: או פונקציית Postgres אטומית להחזר שה-Worker קורא לה רק במסלול שבו הוא יודע שלא שולם (`failure: 'upstream'` עם `status: null`, כלומר הבקשה לא הגיעה בכלל), או ויתור מוצהר. הטסט `the failed call still consumed one quota unit, by design` ב-`backend/worker/src/voice.test.ts` נועל את ההתנהגות הקיימת, וישתנה יחד עם התיקון במקום שהשינוי יעבור בלי ששמו לב.
- **ל-`POST /ai/voice` אין CORS, ולכן הווב אינו יכול לקרוא לו בכלל.** ה-Worker אינו עונה על preflight מסוג `OPTIONS` ואינו שולח `Access-Control-Allow-Origin`, כלומר כל קריאה מהדפדפן תיחסם עוד לפני שתגיע לשומר הסף. בנייד זה לא חוסם, `fetch` ב-React Native אינו כפוף למדיניות המקור, ולכן חיווט הנייד יכול להתקדם בלי זה. **לא נבנה בצעד 6 כי חיווט הקליינט הוא צעד נפרד**, ומדיניות מקור שנכתבת לפני שידוע מאיזה דומיין הווב ייפרס היא או רחבה מדי (כוכבית על נקודת קצה שעולה כסף אמיתי) או פשוט שגויה. כשמחווטים את הווב: רשימת מקורות סגורה ולא כוכבית, ו-`Access-Control-Allow-Headers: Authorization`, שבלעדיו הטוקן לא עובר גם כשהמקור מאושר. **עדכון, החלטת היזם 2026-09-02: הקלטה קולית היא נייד בלבד**, ולכן CORS אינו נדרש לקול כלל. הוא יידרש רק ביום שקבלות OCR ייכנסו לווב, ואז לפי אותם שני כללים.
- **הזנת צפי ההכנסה קבורה שלוש לחיצות עמוק, דיווח היזם 2026-09-02.** המסלול היום הוא טאב חלקות ← לחיצה על החלקה ← טאב "הכנסה" ← כפתור "עדכון צפי", ואין שום נקודת כניסה ממסך הבית. זה השדה שקובע את **כל** תמונת הרווח של החלקה, כלומר הוא גם החשוב ביותר וגם הקבור ביותר, וחלקה בלי צפי פשוט לא מייצרת מספר. זה מקרה פרטי חריף של הפריט "יותר מדי לחיצות" למעלה, ולכן לא לתקן אותו לבד בהוספת עוד קיצור דרך נקודתי למסך הבית: זה בדיוק מה שכבר נעשה בקיצורי הדרך של הנייד ולא הזיז את הבעיה. לטפל בו בתוך סבב ספירת הלחיצות, כשעוברים מסלול-מסלול. **מה שכן שווה למדוד שם:** כמה חלקות במשק אמיתי נשארות בלי צפי בכלל, כי זה המספר שאומר אם מדובר בנוחות או בכשל אימוץ.
- **הווב מתפצל לשני דומיינים, החלטת היזם 2026-09-02, אושרה ולא נבנתה.** `yevul.app` יהיה אתר תדמיתי סטטי עם דף נחיתה כשער כניסה, תפריט ניווט סטנדרטי וכפתור "התחברות", ו-`app.yevul.app` יהיה המערכת עצמה בדיוק כפי שהיא היום. **הנימוק הוא SEO ו-GSO**: הווב הנוכחי הוא Vite SPA, כלומר HTML ריק שמתמלא בג'אווהסקריפט, וזה בסיס חלש לדף שאמור להיסרק. סורקי ה-AI גרועים בזה הרבה יותר מגוגל. **הפיצול אינו נוגע במוצר**: לא בסופאבייס, לא ב-RLS, לא ב-Worker ולא במסך אחד, רק בדומיין ובקישור. שלב נפרד ברודמאפ, אינו חלק משלב 5. תלונה נפרדת שעלתה באותה נשימה ואינה מטופלת בפיצול: **האפליקציה בדפדפן נראית כמו טלפון ולא כמו אתר**, וזה שייך לפריטי הרספונסיביות ו"יותר מדי לחיצות" למעלה.
- **רשומות שנכתבו לפני תיקון התאריכים נשארו עם היום השגוי, ואי אפשר לזהות אותן בדיעבד.** הבאג תוקן ב-2026-09-02, אבל כל `expenses.date`, `log_entries.date` ו-`tasks.due_date` שנכתבו מהנייד בבחירת יום/חודש נשמרו **יום אחד אחורה**, בלי תלות בשעה. רשומות מהווב, ורשומות נייד שלקחו את ברירת המחדל "היום", שגויות רק אם הוזנו בין חצות ל-02:00 או 03:00 מקומית. **אין בסכימה שום עמודה שאומרת איזה קליינט או איזה מסלול כתב שורה**, ולכן אי אפשר להבחין בין השניים, ותיקון גורף של `date + 1` היה הורס דווקא את השורות התקינות. **מה שקריטי לבדוק ידנית: תאריכי ריסוס.** תאריך ריסוס שנשמר יום מוקדם מדי גורם ל-`safeHarvestDate` להכריז "בטוח לקטיף" **יום אחד לפני הזמן**, וזה הכיוון המסוכן. אם יש במשק רשומות ריסוס אמיתיות מהנייד, לעבור עליהן מול מה שהחקלאי זוכר לפני שסומכים על חלון קטיף שנגזר מהן.
- **`openSafeHarvestDate` בונה את "היום" שלו מ-`getUTC*` על שעון חי, ולא שונה בתיקון התאריכים של 2026-09-02.** התיקון החליף `toISOString().slice(0,10)` בכל מקום שבו ה-Date הגיע משעון קיר, אבל את הפונקציה הזו השארנו, במכוון: היא הפונקציה הרגולטורית, והשינוי בה הוא **שינוי התנהגות ולא תיקון פורמט**. התסמין: בין חצות ל-02:00 או 03:00 שעון ישראל, UTC עדיין באתמול, ולכן צ'יפ "בטוח לקטיף מ-" ממשיך להופיע בשעות הראשונות של היום שבו הוא כבר אמור להיעלם. **בישראל השגיאה שמרנית**, היא חוסמת יותר מדי ולא פחות מדי, ולכן היא לא מסוכנת. **בכל אזור זמן ממערב ל-UTC היא מתהפכת ומתירה קטיף יום מוקדם מדי**, שזה הכיוון המסוכן. כלומר זה חסם אמיתי ליום שבו `locale_code` יתמוך במשק מחוץ לישראל, ולא סתם ניקיון. להכריע בנפרד ובמודע, עם טסט שנכתב לפני השינוי.
- **סבב הערות מעידו, חקלאי, 2026-09-02. אלה דרישות מוצר ולא תקלות, ואף אחת מהן לא נבנתה.** לפי סדר עלות עולה: **(א)** להוסיף את שם הגידול למסך עריכת פרטי החלקה. **(ב)** לאפשר הזנת מחיר לקילו גם כשיחידת היבול היא טון, כלומר המרה בין היחידות בצפי ולא יחידה אחת כפויה. **(ג)** בלוח המשימות, לבחור סוג משימה ואז חלקה מרשימה נפתחת בסרגל, במקום להיכנס לכל חלקה בנפרד כדי לפתוח לה משימה. זהו מקרה פרטי של "יותר מדי לחיצות" למעלה. **(ד)** יומן משק כללי מעל יומני החלקות, שמרכז את כל הרשומות ומופיע בסרגל. **(ה)** מחירון חומרי ריסוס לפי עלות לליטר או לקילו: החקלאי מזין סוג חומר וכמות, והאפליקציה מחשבת את עלות הריסוס. **נבנה ואומת 2026-09-04, ראה הפריט הייעודי למטה.** **(ו)** שעות עבודה וסוג עבודה ביומן, שנגזרות אוטומטית לעלות עבודה בהוצאות. **עדיין פתוח**, ותלוי בסתירה הפתוחה עם עידו על "כסף = חשבוניות בלבד" (הפריט למטה), כי (ו) כותב הוצאה בעוד (ה) לא. אותה מכונה של "כמות כפול מחיר יחידה קפוא על השורה" כבר קיימת מ-(ה) ומחכה לו. **(ז) לא ברור ולא תורגם: "מיקומים".** נשאל ולא נענה.
- **סתירה פתוחה בין עידו לבין המוצר הבנוי, 2026-09-02, ללא הכרעה.** עידו אומר: "כסף = הנהלת חשבונות, מתבסס אך ורק על חשבוניות הוצאות והכנסות ולא על נתונים שהחקלאי מזין, לא צריך להופיע בסרגל, משהו שלא מזינים כל יום אלא פעם בחודש חודשיים". **המוצר בנוי הפוך**: כסף הוא טאב ראשי, וההוצאות מוזנות ידנית על ידי החקלאי מאז שלב 3. **המחיר של ההכרעה הזו גבוה משנראה**: אם עידו צודק, `VoiceExpense` בשלב 5, על הסכמה, הוולידטור והטסטים שלה, בנויה למסלול שלא קיים. הנימוק שנשלח לעידו ולא נענה עדיין: המוצר קיים כדי שהחקלאי יידע כמה הוא מרוויח **תוך כדי העונה**, וחשבונית מרואה חשבון מגיעה חודשיים אחרי שההחלטה כבר התקבלה. **לא לבנות עוד שום דבר שתלוי בהזנת הוצאה ידנית עד שיש תשובה.** צעד 8 בשלב 5 אינו תלוי בה: הוא מנגנון ההקלטה עצמו, שנדרש גם ליומן וגם למשימה.
- **שלוש פינות שנשארו פתוחות אחרי מעבר הרשימות ל-FlatList ב-2026-09-02.** **(א) `PlotDetailScreen` מריץ שני מופעים נפרדים של `useExpenses`**, אחד ב-`usePlotExpensesTotal` במסך עצמו ואחד בתוך ה-`ExpenseList` בטאב ההוצאות. הוספת הוצאה מרעננת את הרשימה ומשאירה את כותרת הרווח שמעליה מיושנת עד שחוזרים למסך. הכפילות היא הבאג, לא הרענון, ומשיכה לרענון רק נותנת דרך מילוט ידנית. **(ב) ל-`useFarmSettings` אין `refresh` בכלל**, ולכן מטבע ויחידות שטח אינם נטענים מחדש במשיכה. **(ג) `SettingsScreen` לא קיבל משיכה לרענון** כי הוא בנוי על `FormScreen`, ושם יושב טיפול המקלדת שלא נגעו בו במכוון. **(ד) אותה תכונה של `loading` שנשאר false ברענון קיימת גם ב-`frontend/web`**, שלא נגעו בו כאן, וכל פתרון רענון בווב ייתקל באותה בעיה בדיוק ויוכל להשתמש ב-`loadCount` שכבר קיים ב-`packages/shared/src/refresh.ts`.
- **החלפת קבלה לא שומרת את הקודמת, ובמקרה אחד משאירה קובץ יתום לנצח.** `attachReceipt` כותב לנתיב `{farm_id}/{expense_id}.{ext}` עם `upsert: true`, כלומר **הנתיב נגזר מההוצאה ולא מהקבלה**. התוצאה תלויה בסיומת: החלפת JPEG ב-JPEG **דורסת את הבייטים**, והשורה שסומנה כמחוקה ב-`receipts` מצביעה מעכשיו על הקובץ החדש, כלומר ההיסטוריה משקרת. החלפת JPEG ב-PDF משאירה את הקובץ הישן **באחסון לתמיד**: הנתיב שונה, אף אחד לא קורא ל-`receipts_storage_delete`, והוא עדיין נשמר, עדיין נספר בחשבון, ועדיין ניתן לחתימה למי שיודע את הנתיב. **הצופה בקבלה אינו נשבר מזה** כי הוא תמיד פותר את השורה החיה. הכיוון כשנוגעים: נתיב שנגזר ממזהה הקבלה ולא ממזהה ההוצאה, ואז החלפה היא קובץ חדש ומחיקה מפורשת של הישן, במקום דריסה. שייך לאותה משפחה של מכסת האחסון ומעקב העלות, הפריט האחרון בשלב 5.
- **`design.md` שורה 1039 מסמנת את הצופה בקבלה כ-Not specified, וזה עדיין נכון אחרי שהוא נבנה.** השורה מפרטת "Receipt Thumbnail, full-screen document viewer, file upload affordance" ומסמנת ⚠️. הצופה נבנה ב-2026-09-02 לפי שפת הגיליונות והמודאלים שכבר קיימת במוצר (כפתור משני Field-700, רקע מסמך Mist-100), **ובמכוון לא ב-Wheat**, כי `design.md` שורה 94 שומרת את Wheat 500 לאפשרויות **סריקה**, ופתיחת מסמך שכבר תויק אינה סריקה. לעדכן את השורה ההיא כשמגיעים לליטוש התיעוד, כדי שהמפרט יתאר את מה שקיים.
- **מחיקת הוצאה נבנתה, ונשאר ממנה חוב אחד: אין Undo.** `deleteExpense` ב-`packages/shared/src/expenses.ts` הוא מחיקה רכה יחידה על `expenses.deleted_at`, בדיוק בצורת `deleteTask`, וכפתור המחיקה יושב בשורת ההוצאה בשני הלקוחות באותו מקום שבו הוא יושב בשורת המשימה, עם אותו `ConfirmDialog` לפני הכתיבה. **הוכרע ולא נשאר פתוח:** ההקצאות ושורת ה-`receipts` לא נוגעים בהן (הכסף לעולם לא יושב על ההקצאה, וכל קריאה שלהן היא join תחת שאילתה על `expenses` שכבר מסננת `deleted_at`), והקובץ באחסון **לא נמחק** אף שהמדיניות `receipts_storage_delete` מתירה זאת. **מה שנשאר פתוח:** `design.md` דורש "מחיקה עם ביטול שנשאר חמש שניות, בכל מקום שיש מחיקה" (וגם שלב 8 ברודמאפ), ואין Undo כזה לא במשימה ולא בהוצאה. כלומר זה חוב אחד משותף לשתיהן, לא חוב חדש שנוצר כאן, וכשהוא ייסגר הוא צריך להיסגר לשתיהן באותה נשימה. **נבדק רק בטסטים של `packages/shared`, לא על מכשיר ולא בדפדפן.**
- **ההחלקה לסגירה בגיליון התחתון נבנתה ולא נבדקה על מכשיר.** היזם דיווח שהיא "לא חלקה, לפעמים עובדת לפעמים לא", והאמת הייתה שהיא לא הייתה קיימת כלל: `BottomSheet.tsx` הציג גבשושית אחיזה שלא הייתה מחוברת לשום דבר, ומה שנראה כמו "לפעמים עובד" היה אצבע שנחתה על ה-scrim, שכן נסגר בלחיצה. הג'סטורה מומשה עכשיו עם `Animated` ו-`PanResponder` בלבד, בלי reanimated ובלי gesture-handler, אותו נימוק כמו שאר הקובץ. **החשבון עצמו נבדק** ב-`packages/shared/src/sheetDrag.test.ts`, 24 טסטים על המרחקים, המהירות, משך היציאה ודהיית ה-scrim. **מה שלא נבדק הוא כל מה שדורש אצבע**: אם התנועה מרגישה חלקה, אם 96 נקודות הן המרחק הנכון לאצבע של בן 65 עם כפפה, ואם הסף לא מחמיר או מקל מדי בשדה. **שתי מגבלות ידועות ומכוונות:** תפיסה של הידית באמצע חזרת הקפיץ מקפיצה את הגיליון למעלה תחילה, כי אי אפשר לקרוא ערך שרץ על ה-native driver בחזרה ל-JS בלי מאזין שעולה הודעת גשר לכל פריים, וסגירה בהחלקה כשהמקלדת פתוחה מחליקה את הגיליון אל מאחורי המקלדת למשך היציאה. **החלטה שכדאי לזכור אם מישהו ירצה להרחיב:** ההחלקה מתחילה רק מהידית שבראש הגיליון ולא מהתוכן, כי החלקה מהתוכן דורשת מטפלים בשלב ה-capture, ואלה בדיוק היו גונבים את גלילות הצ'יפים האופקיות ב-`TaskSheet`, `LogEntrySheet`, `CompletionPromptSheet` ו-`VoiceConfirmPanel`.
- **דפוס המשבצות (`TilePicker`) קיים עכשיו בשלושה מסכים, ועדיין לא בכולם.** הצרכנים הם `SprayEntrySheet.tsx` (יומן ריסוס, נייד בלבד), `PlotFormScreen` (הוספה ועריכה של חלקה, **נייד וגם ווב**, אחרי אישור היזם על התבנית) ו-`ExpenseSheet` (**נייד וגם ווב**). הווב קיבל רכיב `TilePicker` משלו, `frontend/web/src/components/TilePicker.tsx`, עם אותו API בדיוק. **`LogEntrySheet` לא נגעו בו** והוא עדיין הבעלים של טאב היומן, של גיליון הרישום המרכזי ושל תשעת סוגי הרשומה האחרים, כלומר שם עדיין יש סליידרים אופקיים של צ'יפים — בדיוק מה שהיזם קרא לו "הדבר הכי לא נוח". המסכים הבאים בתור: גיליון הרישום (`CaptureSheet`), `TaskSheet` (סוג משימה + חלקה), וגם המקבילים שלהם בווב. **מה שלא נבדק בשום צורה: הכל מה שדורש אצבע.** אם משבצת בגובה 64 באמת נוחה לבן 65, אם רצף השאלות מרגיש ארוך מדי, ואם דילוג על שאלה שכבר יש לה תשובה (מינון בריסוס, יחידת שטח בחלקה) מרגיש כמו חיסכון או כמו משהו שנעלם. הלוגיקה מכוסה ב-`packages/shared/src/sprayEntry.test.ts` (50), ב-`packages/shared/src/plotForm.test.ts` (40) וב-`packages/shared/src/expenseForm.test.ts` (29), הרינדור והמגע לא מכוסים כלל, כי לשני הלקוחות אין מריץ טסטים.
- **`ExpenseSheet` הוא הצרכן הראשון של דפוס המשבצות שנשאר טופס אחד ולא הפך להליכה, וזו הכרעה שנספרה ולא הוכרעה בטעם.** הוצאה היא הפעולה התכופה ביותר במוצר, ולכן נספרו הלחיצות לשתי החלופות לפני שנבחרה צורה: הליכה עולה 4, 6 ו-5 לחיצות בשלושה מקרים, והגיליון הנוכחי עולה 4, 5 ו-4. הספירה המלאה יושבת בכותרת של `packages/shared/src/expenseForm.ts`. **רק שדה אחד הפך למשבצות, שם ההוצאה**, מתוך היסטוריית המשק עצמו (`useExpenseSuggestions`), והסכום נשאר מוקלד במכוון, מאותו נימוק שבגללו שטח החלקה לא הפך לרשת. **מה שלא נמדד: הגובה.** רשת של שלוש שורות ריבועים יושבת עכשיו מעל לוח שנה מלא בתוך אותו גיליון תחתון, כלומר הגלילה עד כפתור השמירה ארוכה יותר מבעבר, ואף אחד לא ראה את זה על מכשיר. אם זה יתברר כבעיה, הכיוון הזול הוא `EXPENSE_NAME_TILE_LIMIT` (חמש היום, שלוש הופכות אותה לשתי שורות), ולא ויתור על הרשת.
- **שתי מחרוזות כפולות שנוצרו בהמרת טופס החלקה, `spray.stepOf` מול `common.stepOf` ו-`spray.confirm` מול `common.confirm`.** מונה הצעדים וכפתור האישור שייכים לדפוס המשבצות ולא למסך אחד, ולכן הגרסה החדשה שלהם נכנסה תחת `common.`. `SprayEntrySheet` עדיין קורא את שלו תחת `spray.`, ולא שונה במכוון, כדי לא לגעת במסך שכבר אושר על מכשיר. לאחד כשנוגעים בו ממילא: שתי שורות ב-`SprayEntrySheet.tsx` ומחיקת שני מפתחות מ-`i18n.ts`.
- **שורת "בטוח לקטיף" מוצגת דרך `new Date(...).toLocaleDateString`, בשלושה מקומות עכשיו.** `LogEntrySheet` בנייד, `LogEntrySheet` בווב, ועכשיו גם `SprayEntrySheet`. `safeHarvestDate` מחזיר `YYYY-MM-DD`, ו-`new Date` על מחרוזת כזו הוא חצות UTC: בישראל זה מוצג נכון, **ממערב ל-UTC זה יוצג יום אחד אחורה**. זו אותה משפחה בדיוק כמו הפריט על `openSafeHarvestDate` למעלה, וזו הסיבה שהעותק החדש נכתב זהה לשניים הקיימים במקום להתפצל מהם: כשמתקנים, מתקנים דפוס אחד בשלושה מקומות. תצוגה בלבד, לא נוגע במה שנשמר.
- **בהוצאה קיימת אפשר לצרף קבלה רק מהגלריה, לא מהמצלמה.** `onPickImage` ב-`frontend/mobile/src/components/ExpenseSheet.tsx` קורא ל-`launchImageLibraryAsync` בלבד. **זה בדיוק הפוך מהמצב בשדה**: החקלאי מחזיק את הנייר ביד ורוצה לצלם אותו, ובמקום זה הוא נשלח לגלריה לחפש תמונה שעדיין לא צילם. `launchCameraAsync` כבר בשימוש ב-`useReceiptScan.ts` יחד עם בקשת הרשאת המצלמה, כלומר כל התשתית קיימת וזו הוספה של בורר בין שתי הדרכים. שווה לאחד את שתי נקודות הכניסה לאותו רכיב, כי אחרת יש שני מקומות שבוחרים תמונה ורק אחד מהם יודע לצלם.
- **סבב הערות שני מעידו, 2026-09-04, והפעם עם הקשר שמשנה את קריאתן: עידו עובד בדפדפן על הגרסה הפרוסה, לא באפליקציה.** לכן ההערה הראשונה שלו, "לא ניתן להזין יומן ריסוסים", **כבר תוקנה יומיים קודם** ופשוט לא הגיעה אליו: הריפו היה 32 קומיטים לפני `origin/main`, כלומר הבילד שהוא בדק קדם לכל שלב 5. **לפני שמבקשים ממנו לבדוק משהו, לבדוק `git rev-list --count origin/main..main` ולדחוף.** שתי ההערות החדשות: **(א)** גלילה ימינה־שמאלה למעבר בין חלקות. שייך לסבב העיצוב ולא לתיקון נקודתי, ובמיוחד כי בדפדפן בטלפון ג'סטורה אופקית מתנגשת עם ניווט אחורה של הדפדפן עצמו. **(ב) מחירון חומרי ריסוס**, חזרה על פריט (ה) מהסבב הראשון ועכשיו מפורט יותר: החקלאי מזין מחיר לק"ג פעם אחת, וביומן הריסוס הוא מזין שם חומר וכמות והאפליקציה מחשבת את העלות. **נבנה ואומת 2026-09-04, ראה הפריט הייעודי למטה.**
- **מחירון חומרי ריסוס, נבנה ואומת 2026-09-04. ארבע ההכרעות שהוכרעו לפני SQL, ומה שנשאר פתוח ממנו.** ההכרעות: (1) **מחיר הוא היסטוריה ולא ערך**, העלות נשמרת קפואה על שורת הריסוס (`log_entries.spray_cost`), ומחירון `spray_material_prices` הוא רק ברירת מחדל שנזכרת לפי חומר, בדיוק כמו `task_cost_memory`. (2) `spray_dose` נשאר טקסט חופשי לרגולציה, ונוספו `spray_quantity` + `spray_quantity_unit` (דומיין `spray_unit`, ליטר/קילו) שאותם כופלים במחיר. (3) נכנס כמשבצת `cost` אחרי `material` בהליכה, עם דילוג-כשידוע על המחיר. (4) **תמיד אפשר להזין עלות ידנית**, בקשה מפורשת של היזם, `spray_cost` תמיד ניתן להקלדה ישירה וגובר על החישוב. עלות ריסוס **מורידה את הרווח** (החלטת היזם), נספרת ישירות משורת הריסוס ולא כהוצאה, כדי לא להיכנס לסתירה הפתוחה עם עידו. אומת בדפדפן ובמסד: חישוב, דריסה ידנית, שמירה, זכירת מחיר, ורווח שירד ב-120. **מה שנדחה במכוון ולא נבנה:** (א) **מיסוך `spray_unit_price`/`spray_cost` מ-worker.** הן עמודות כסף על טבלה תפעולית, אבל log_entries עדיין נקרא ישירות בלי view ממסך, והוספת `log_entries_view` היא שכתוב מסלול הקריאה הכי נפוץ. worker mode לא קיים עד שלב 6, ואם worker רואה עלות ריסוס היא החלטת עיצוב של שלב 6. לסגור אז, באותה תבנית כמו `crop_cycles_view`. טבלת המחירון עצמה כן חסומה מ-worker ברמת השורה. (ב) **סכמת היומן בקול לא מחלצת כמות/עלות.** `VoiceJournal` ב-`voice.ts` עדיין לא נושא אותן, ריסוס בקול נשמר בלי עלות בדיוק כמו קודם. להרחיב את הטיפוס, סכמת ה-JSON והוולידטור יחד כשחוזרים לקול. (ג) **הרווח יורד אבל אין עדיין שורת פירוק "עלות ריסוס"** בכרטיסי הרווח, רק המספר יורד. `FarmProfitForecast.sprayCosts` ו-`PlotProfitForecast.sprayCost` כבר נחשפים לתצוגה כשרוצים. (ד) עלות הריסוס ב-`LogRow` (שני הלקוחות) מוצגת כמספר בלי סימן מטבע, כי `LogRow` לא מקבל `currency`. (ה) **פריט (ו), שעות עבודה,** בונה על אותה מכונה ועדיין פתוח, תלוי בהכרעת הסתירה עם עידו.
- **שלב 6, משימה 1 (הזמנת חברים), נבנתה 2026-09-04. מה שנדחה במכוון.** (א) **אין מייל הזמנה אמיתי**, החלטת היזם 2026-09-04: הבעלים מוסיף אימייל+תפקיד, ו-`handle_new_user` מצרף את המוזמן ברגע שהוא נכנס עם אותו אימייל. אין ספק מייל ואין קישור, והבעלים צריך לומר למוזמן מחוץ לאפליקציה להיכנס עם אותה כתובת. מייל אמיתי דורש ספק מייל ופריסת Worker, ויתווסף אחר כך בלי לשבור כלום. (ב) **משתמש שכבר נרשם ואז הוזמן** יהיה חבר בשני משקים (האוטומטי שלו + המזמין), ו-`currentFarmQuery` (המשק הישן ביותר) יבחר את שלו ולא את המזמין. אין עדיין בורר משקים, וזה תרחיש קצה כי המסלול הרגיל הוא הזמנה לפני הרשמה. `currentFarm.ts` כבר מסמן שבורר משקים, כשיידרש, נכנס שם. (ג) **שיתוף הוא פיצ'ר בתשלום** (מסלול משק, prd.md סעיף 13), אבל **הפיירוול נבנה בשלב 7** (RevenueCat). כאן נבנה המנגנון בלבד, בלי שער תשלום ובלי הגבלת "עד ארבעה אנשים", שתיהן אכיפת שרת של שלב 7. (ד) **`farm_members_view` חושף אימייל של כל חבר לכל חבר פעיל**, כולל worker, תואם למדיניות הרוסטר הקיימת ("לא מידע רגיש"). אם אי פעם יוחלט שעובד לא רואה את שאר הרוסטר, לצמצם את ה-view ל-owner/manager. (ה) **טרם נבדק על מכשיר / בדפדפן בפועל.** נבדק ב-build, typecheck, lint, 15 בדיקות pgTAP ו-12 בדיקות יחידה, אבל האימות הוויזואלי בשני הלקוחות עדיין לא נעשה (גישת הדפדפן בסשן לא אושרה, והנייד דורש בדיקת מכשיר של היזם). לא לסמן `[x]` ברודמאפ עד שנבדק בפועל.
- **שלב 6, שאר המשימות נבנו 2026-09-05 בתתי-סוכנים מקבילים (מצב עובד, שיוך אנשים, אווטרים, מתג "החלקות שלי"). כל השלב שלם בקוד, כל הבדיקות ירוקות (660 יחידה, 131 pgTAP), אבל טרם נבדק על מכשיר/בדפדפן.** פערים שנרשמו במכוון: **(א)** מסך הבית לא מרנדר כרטיסי חלקה נפרדים אלא כרטיס רווח מצרפי + לוח משימות, ולכן מתג "החלקות שלי" מסנן כרגע רק את הלוח (design.md מדבר גם על סינון כרטיסי חלקה, שעדיין לא קיימים בבית). **(ב) אווטר:** הנתיב קבוע לפי סיומת (`avatar.jpg`), ולכן העלאה מחדש מאותו סוג עלולה להגיש תמונה ישנה מה-CDN; הלקוחות עוקפים עם תצוגה מקומית עד רענון. תמונה קטנה שאינה jpeg/png/webp שה-canvas לא נגע בה תידחה בדלי ותציג `members.avatar.error`. **(ג)** RLS של אובייקטי סטורג' לאווטרים לא נבדק ב-pgTAP (כמו `receipts_storage_*`, לא מעשי לדמות סכמת storage ב-pgTAP). **(ד) לקח אורקסטרציה:** בידוד worktree של תתי-הסוכנים נוצר מנקודת ההתחלה של הסשן ולא מ-main הנוכחי, מה שגרם לאיחוד תלת-כיווני יקר ב-`members.ts`. גל 2 הורץ מ-main הנוכחי בלי בידוד וזה עבד חלק. בפעם הבאה: או סוכנים לא-מבודדים מהמצב הנוכחי עם בעלות קבצים נפרדת, או ליצור worktree ידני מ-main. **(ה) רפקטור יעילות שנדחה מ-`/simplify`:** `useMembers`/`useMyRole` הם הוקים לא-מקושרים, וכל מסך קורא את `farm_members_view` 3-4 פעמים (App+Sidebar תמיד מותקנים, ועוד HomeScreen+TaskBoard). הכיוון: ספק Context אחד ברמת המעטפת המאומתת שמחזיק `{ members, myRole, currentUserId, refresh }` פעם אחת, בדיוק בצורת `AuthProvider` הקיים, וכל הצרכנים קוראים ממנו. בנוסף בווב מסך הבית מושך `usePlots` בנפרד בזמן ש-`ProfitHeroCard` כבר מושך חלקות דרך `useFarmProfit` (הנייד מאחד דרך `profit.plots`), לחקות את הנייד. נדחה במכוון: הטבלה זעירה (עד ~5 חברים), והכנסת Context בסוף שלב בדוק מסכנת אותו. לבנות כרפקטור ייעודי, לא כטלאי.
- **קישור "יומן ריסוס" יושב במסך ההגדרות בווב, והיזם שאל למה, 2026-09-04.** זו נקודת כניסה שלישית שנבחרה בשלב 3 כי לווב אין טאב "עוד" (design.md ממקם את השלישית ב"עוד/הגדרות"). היזם צודק שזה מוזר, יומן ריסוס אינו הגדרה. לטפל בסבב הניווט/עיצוב: להעביר את נקודת הכניסה למקום טבעי יותר בווב (סרגל הצד ליד "יומן", או מתוך מסך היומן). לא תוקן עכשיו לבקשת היזם ("נטפל בזה אחר כך").
- **תמונת פרופיל / אווטר למשתמשים, בקשת היזם 2026-09-04.** design.md, Member Avatar, קבע במפורש "No photo uploads in V1" (בורר תמונה הוא חיכוך לפיצ'ר שרק צריך להבחין בין שניים-שלושה אנשים). היזם מבטל את ההחלטה הזו ורוצה העלאת תמונה. דורש: באקט אחסון לאווטרים עם RLS לפי חברות במשק, העלאה בשני הלקוחות, חשיפת `avatar_url` ב-`farm_members_view`, ורינדור התמונה במקום ראשי התיבות (עם נפילה לראשי תיבות כשאין תמונה). לבנות כצעד ייעודי בשלב 6 אחרי שזרימת ההזמנה אומתה.
- **חוב ניקיון לפני השקה אמיתית, נפתח 2026-09-06. יש כאן יותר מפריט אחד, וכולם חייבים להיסגר לפני שמשתמש משלם נכנס.** (א) **נזרעו נתוני דמו על מסד הפרודקשן**, באישור מפורש של היזם, כדי שהוא ועידו יוכלו לבדוק מול הבילד החי. הם יושבים אך ורק במשק `3e2cd5b4-f3f2-4b1b-b434-6ce3d89b44a9` (המשתמש `demo-owner@yevul.app`), חמש חלקות, חמישה מחזורי גידול, תשע משימות, שמונה רשומות יומן והוצאה אחת. **למחוק את המשק הזה ואת המשתמש שלו לפני השקה.** אין להם שום קשר לנתונים של עידו. (ב) **כפתור "כניסה כמשתמש בדיקה" חשוף בפרודקשן** דרך `VITE_DEMO_LOGIN=true` ב-Vercel (`SHOW_DEMO_LOGIN` ב-`LoginScreen.tsx`). זו דלת אחורית לחשבון משותף ידוע, והיא מבלבלת: מי שנכנס דרכה רואה את משק הדמו ולא את שלו, וזה בדיוק מה שקרה ליזם ולעידו. לכבות את המשתנה בפרודקשן לפני השקה. (ג) לעבור על שאר שאריות הפיתוח (seed, קבצי בדיקה, מפתחות) באותה נשימה.
