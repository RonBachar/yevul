# משתני ה-Worker. בפיתוח מקומי הם יושבים ב-backend/worker/.dev.vars
# (לא נכנס ל-git), ובפרודקשן נקבעים עם `wrangler secret put`.
# הרצה מקומית: npx wrangler dev --port 8788 מתוך backend/worker

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENROUTER_API_KEY=
REVENUECAT_WEBHOOK_SECRET=
